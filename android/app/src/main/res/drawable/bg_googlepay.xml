<?xml version="1.0" encoding="utf-8"?>
<!-- Google Pay button background (demo) — white fill with a thin border,
     matching Google Pay's own brand guidance for a "light" button variant. -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight"
    android:radius="16dp">
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/googlepay_white" />
            <stroke android:width="1dp" android:color="@color/googlepay_border" />
            <corners android:radius="12dp" />
        </shape>
    </item>
</ripple>
