{
  "name": "braintree-android-demo-server",
  "version": "1.0.0",
  "private": true,
  "description": "Local Node server backing the Android Braintree demo app — reached via adb reverse tcp:3000 tcp:3000, not a browser.",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "braintree": "^3.22.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
